{
  "name": "safeguard-hub-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore security-rules tests for Safeguard Hub (emulator-only; touches no production data).",
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-safeguard-hub-rules \"node --test tests/firestore-access.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0",
    "firebase-tools": "^13.29.0"
  }
}
